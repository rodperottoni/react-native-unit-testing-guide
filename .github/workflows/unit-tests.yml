name: 'Unit Tests'
on: [push]

permissions:
  contents: read
  
jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
    - name: 'Checkout Repository'
      uses: actions/checkout@v3
    - name: 'Install modules'
      run: yarn
    - name: 'Run tests'
      run: yarn test
